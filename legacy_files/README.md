# Legacy Files

This folder is for local-only legacy materials that should not be pushed to the remote repository.

Current local contents:

- `gpt_screenshots/*.png`: screenshot notes from earlier GPT discussion. These files are used as historical context for the project story and are intentionally ignored by Git.

The collaborator-facing synthesis based on these screenshots is tracked separately in `COLLABORATOR_PROJECT_STORY.md`.
